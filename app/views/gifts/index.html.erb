<h1>Listing gifts</h1>

<table>
  <tr>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @gifts.each do |gift| %>
  <tr>
    <td><%= gift.description %></td>
    <td><%= link_to 'Show', [gift.user, gift] %></td>
    <td><%= link_to 'Edit', edit_user_gift_path(gift.user, gift) %></td>
    <td><%= link_to 'Destroy', [gift.user, gift], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Gift', new_user_gift_path(params[:user_id]) %>
